import * as init from '$functions/init.ts';
// import { setContext, getContext } from 'svelte';

export function returnProps(props) {
  return props;
}

// export function returnProps(map) => {
//   const tempObj = {}

//   map.forEach((element) => tempObj[element] = 'test');

//   return tempObj;
// }